# MongoDB connection string (required for save/load; the editor itself works without it)
MONGODB_URI=mongodb://localhost:27017

# Optional overrides
# MONGODB_DB=fractal-editor
# MONGODB_COLLECTION=fractals
# PORT=3000
